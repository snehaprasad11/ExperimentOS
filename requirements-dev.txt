# Everything the backend needs, plus tools for tests, stats validation,
# real-data analysis, and the Kaggle pull.  Install for local development:
#   pip install -r requirements-dev.txt
-r requirements.txt
statsmodels>=0.15    # cross-checks the stats core in tests
pandas>=2.0          # analysis scripts
kaggle>=1.6          # reproducible dataset pull
pytest>=8.0
httpx>=0.27          # FastAPI TestClient
